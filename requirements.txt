fastapi==0.115.0
uvicorn[standard]==0.30.6
git+https://github.com/apt-team-018/transformers-usf-exp.git
accelerate
hf_transfer
# Note: transformers will be installed from git repository in Dockerfile
safetensors>=0.4.4
sentencepiece>=0.2.0
einops>=0.7.0
# VLM-specific dependencies
Pillow>=10.0.0
aiohttp>=3.9.0
orjson>=3.9.0
# Optional for FlashAttention 2 (speeds up attention on H100). If it fails to build, server falls back to SDPA.
# flash-attn==2.6.1